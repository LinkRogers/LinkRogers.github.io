# 个人主页使用指南

## 这个个人主页是怎么工作的？

这是一个基于 **Jekyll** 的静态网站，使用 **GitHub Pages** 托管服务。以下是它的工作原理：

### 技术架构

1. **Jekyll** - 一个静态网站生成器
   - Jekyll 使用 Markdown 文件（.md）和 HTML 模板生成静态网页
   - 每次提交代码到 GitHub 后，GitHub Pages 会自动运行 Jekyll 构建网站
   
2. **GitHub Pages** - 免费的网站托管服务
   - 你的网站托管在 GitHub 的服务器上
   - 网址是：`https://LinkRogers.github.io/`
   - 完全免费，无需购买服务器或域名

3. **工作流程**
   ```
   编辑文件 → 提交到 GitHub → GitHub Pages 自动构建 → 网站更新上线
   ```

### 网站结构

```
LinkRogers.github.io/
├── _config.yml          # 网站全局配置文件（重要！）
├── index.md             # 首页内容
├── about.md             # 关于页面
├── CV.md                # 简历页面
├── research.md          # 研究页面
├── project.md           # 项目页面
├── _layouts/            # 页面布局模板
├── _includes/           # 可重用的页面组件
├── _sass/               # 样式文件
├── css/                 # CSS 样式
└── pics/                # 图片资源
```

## 如何更改页面设置？

### 1. 修改基本信息

编辑 `_config.yml` 文件来修改网站的基本信息：

```yaml
title: Shi Li                              # 网站标题
email: shili@saicmotor.com                 # 联系邮箱
description: "你的个人简介..."              # 网站描述
url: "http://LinkRogers.github.io/"       # 网站网址
github_username: LinkRogers                # GitHub 用户名
```

**注意**：修改 `_config.yml` 后，如果在本地测试，需要重启 Jekyll 服务器才能看到变化。

### 2. 修改首页内容

编辑 `index.md` 文件：

```markdown
---
layout: page
title : Shi Li's Personal Homepage
---

在这里编写你的首页内容...
```

- 修改 `title` 来改变页面标题
- 在 `---` 下方编写页面内容（支持 Markdown 和 HTML）

### 3. 修改其他页面

- **关于页面**: 编辑 `about.md`
- **简历页面**: 编辑 `CV.md`
- **研究页面**: 编辑 `research.md`
- **项目页面**: 编辑 `project.md`

每个文件开头都有 YAML front matter（两个 `---` 之间的部分）：

```markdown
---
layout: page              # 使用的布局模板
title: 页面标题           # 页面标题
permalink: /about/        # 页面的 URL 路径
weight: 6                 # 导航栏中的排序权重（数字越小越靠前）
---
```

### 4. 更换头像和图片

1. 将新图片上传到 `pics/` 文件夹
2. 在 Markdown 文件中引用：
   ```markdown
   ![图片描述](/pics/your-image.jpg)
   ```

### 5. 修改样式和布局

- **样式**: 编辑 `_sass/` 和 `css/` 文件夹中的 CSS 文件
- **布局**: 编辑 `_layouts/` 文件夹中的 HTML 模板
- **组件**: 编辑 `_includes/` 文件夹中的可重用组件

### 6. 添加新页面

1. 在根目录创建新的 `.md` 文件，例如 `blog.md`
2. 添加 YAML front matter：
   ```markdown
   ---
   layout: page
   title: 我的博客
   permalink: /blog/
   weight: 3
   ---
   
   这里是博客内容...
   ```
3. 提交到 GitHub，页面会自动生成

## 如何发布更改？

### 使用 Git 命令行

```bash
# 1. 查看修改的文件
git status

# 2. 添加修改的文件
git add .

# 3. 提交更改
git commit -m "描述你的更改"

# 4. 推送到 GitHub
git push origin main
```

### 使用 GitHub 网页界面

1. 访问 https://github.com/LinkRogers/LinkRogers.github.io
2. 点击要编辑的文件
3. 点击右上角的编辑按钮（铅笔图标）
4. 编辑文件内容
5. 滚动到页面底部，填写提交信息
6. 点击 "Commit changes"

**等待时间**: 推送更改后，GitHub Pages 通常需要 1-2 分钟来重新构建网站。

## 本地测试

如果想在发布前本地预览网站：

### 1. 安装 Jekyll

```bash
# 安装 Ruby（Jekyll 的依赖）
# 然后安装 Jekyll
gem install jekyll bundler
```

### 2. 运行本地服务器

```bash
# 进入网站目录
cd LinkRogers.github.io

# 启动 Jekyll 服务器
jekyll serve

# 或者使用 bundle
bundle exec jekyll serve
```

### 3. 访问本地网站

在浏览器打开：`http://localhost:4000`

## 常见问题

### Q: 为什么我的更改没有显示？

A: 
1. 等待 1-2 分钟，GitHub Pages 需要时间构建
2. 强制刷新浏览器（Ctrl+F5 或 Cmd+Shift+R）
3. 检查是否正确推送到了 `main` 分支

### Q: 如何自定义域名？

A: 
1. 在根目录创建 `CNAME` 文件
2. 在文件中写入你的域名，例如：`www.example.com`
3. 在域名提供商处设置 DNS 记录指向 GitHub Pages

### Q: 网站构建失败怎么办？

A:
1. 检查 GitHub 仓库的 "Actions" 标签查看构建日志
2. 确保 YAML front matter 格式正确（缩进和冒号）
3. 确保没有语法错误

## 有用的资源

- [Jekyll 官方文档](https://jekyllrb.com/)
- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [Markdown 语法指南](https://www.markdownguide.org/)
- [YAML 语法指南](https://yaml.org/)

## 联系支持

如果遇到问题：
1. 查看 [Jekyll 疑难解答](https://jekyllrb.com/docs/troubleshooting/)
2. 访问 [GitHub Pages 帮助](https://docs.github.com/en/pages)
3. 在 GitHub 仓库中创建 Issue

---

**祝你使用愉快！** 🎉
